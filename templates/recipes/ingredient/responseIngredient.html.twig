
{% extends "layout/base.html.twig" %}

{% block content %}
    <div class="container">
        <h1>Ingredients</h1>
        <div class="row">
            <ul>
                {% for recipe in recipes %}
                    <li>{{ recipe.title }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}